[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions" # Asegúrate que coincida con tu carpeta de funciones

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200